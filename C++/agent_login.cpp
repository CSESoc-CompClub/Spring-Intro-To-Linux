// This program takes in a password and checks it to ensure only authorized personnel can log into the system

#include <iostream>
#include <string>

int main()
{
    std::cout << "Welcome, Agent. Please enter the secret code to proceed: ";

    // TODO[1]: take in input to scan in secret code


    // TODO[2]: verify that input given is correct
    // If it is correct, print a message to grant access
    // Else we deny access, print an error message
    // You have the decision to decide what counts as a secret code
    

    return 0;
}